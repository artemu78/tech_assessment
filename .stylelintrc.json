{
  "extends": "stylelint-config-standard",
  "rules": {
    "selector-class-pattern": ".*",
    "declaration-block-no-redundant-longhand-properties": [
      true,
      { "ignoreShorthands": ["/grid-template/"] }
    ]
  },
  "ignoreFiles": ["coverage/**", "build/**","coverage-ts/**"]
}
